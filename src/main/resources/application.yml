spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/crawler?serverTimezone=UTC
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
